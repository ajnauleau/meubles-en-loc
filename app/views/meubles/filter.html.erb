
<div class="card-header d-flex justify-content-center">
  <h1>Meubles index</h1>
</div>

<!--<div class="container">
  <div class="row justify-content-center">
    <div class="col-4 col-md-6 col-lg-8">
      <form action="/meubles/filter" method="post">
        <%#= hidden_field_tag :authenticity_token, form_authenticity_token %>
        <h2 class="text-center m-5">Filter your search</h2>
        <div class="form-row">
          <div class="form-group">
            <label for="provider">Category</label>
            <select id="provider" name="category" class="form-control">
              <option value="Bathroom">Bathroom</option>
              <option value="Bedroom">Bedroom</option>
              <option value="Kitchen">Kitchen</option>
              <option value="Living room">Living Room</option>
              <option value="Kids">Kids</option>
            </select>
          </div>
          <div class="form-group">
            <label for="f_name">Description</label>
            <input type="text" id="description" placeholder="Description" class="form-control">
          </div>
          <div class="form-group">
            <label for="l_name">Price</label>
            <input type="text" id="price" placeholder="price" class="form-control">
          </div>
          <div class="form-group">
            <input type="submit" value="Search" class="btn btn-primary btn-lg w-100">
          </div>
        </div>
      </form>
    </div>
  </div>
</div>-->

<div class="container">
  <div class="row justify-content-center">
    <div class="col-4 col-md-6 col-lg-8">
      <form action="/meubles/filter" method="post">
        <h2 class="text-center m-5">Filter your search</h2>
        <%= simple_form_for(@meuble) do |f| %>
            <%= f.input :category, collection: Meuble::CATEGORIES %>
            <%= f.input :daily_rate %>
            <%= f.submit class:'btn btn-primary', value:'Search' %>
          <% end %>
      </form>
    </div>
  </div>
</div>

<div class="container p-4">
  <div class="row d-flex justify-content-center">
    <% @meubles.each do |meuble| %>
      <div class="col-xs-3 col-md-4  d-flex flex-column border m-2 p-2">
        <p><strong><%= meuble.name %></strong></p>
        <p><%= meuble.daily_rate %> Euro</p>
        <p><%= meuble.category %></p>
        <p><%# meuble.photo_id %></p>
        <%= link_to 'Show', meuble_path(meuble) %>
      </div>
    <% end %>
  </div>
</div>

<div class="container p-2 mt-5">
  <div class="row d-flex justify-content-start">
    <a href="/meubles"><div class="btn btn-primary">Back to Index</div></a>
  </div>
</div>
